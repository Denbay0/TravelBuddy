import AdminPageTemplate from './AdminPageTemplate'

export default function AdminLoginPage() {
  return <AdminPageTemplate title="Admin Login" description="Sign in to the admin console (auth flow pending integration)." />
}
